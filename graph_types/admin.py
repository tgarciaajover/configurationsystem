from django.contrib import admin

from graph_types.models import GraphType

# Register your models here.

# Registro del modelo GraphType en el modulo de administrador.
admin.site.register(GraphType)
